[build]

[deploy]
startCommand = "npm run setup && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
RAILPACK_BUILD_APT_PACKAGES = "python3 make g++"
RAILPACK_NODE_VERSION = "22"
